# Enable Append Only File (AOF) persistence.
# Every write command is logged.
appendonly yes

# Flush the AOF to disk after every write.
# Maximizes durability at the cost of write performance.
appendfsync always

# Directory where Redis stores persistence files (AOF/RDB).
# This is mounted to a Docker volume for persistent storage.
dir /data